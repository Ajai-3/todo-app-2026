# Emerald Todo

Local-first Todo app with analytics (heatmap, spider graph), timers, recurring tasks.

## Setup
```
yarn install
yarn dev
```
Open http://localhost:3000

All data stored in browser LocalStorage. No backend or database needed.
